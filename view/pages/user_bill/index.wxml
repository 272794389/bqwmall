<navbar parameter='{{parameter}}'></navbar>
<view class='bill-details'>
   <view class='nav acea-row'>
      <view class='item {{type==0 ? "on":""}}' data-type='0' bindtap='changeType'>全部</view>
      <view class='item {{type==2 ? "on":""}}' data-type='2' bindtap='changeType'>收入</view>
      <view class='item {{type==1 ? "on":""}}' data-type='1' bindtap='changeType'>支出</view>
   </view>
   <view class='sign-record'>
      <view class='list' wx:for="{{userBillList}}" wx:key='index'>
          <view class='item'>
            <view class='data'>{{item.time}}</view>
            <view class='listn'>
                <view class='itemn acea-row row-between-wrapper' wx:for="{{item.list}}" wx:for-item="vo" wx:key='index'>
                  <view>
                      <view class='name line1' style="width:490rpx;"><view style="float:left;width:120rpx;">{{vo.mark}}</view><view style="color:#f00;float:left;" wx:if="{{vo.use_money>0}}">(-{{ vo.fee}}费用,+{{ vo.repeat_point}}重消)</view></view>
                      <view>{{vo.add_time}}</view>
                  </view>
                  <view class='num' wx:if="{{vo.use_money>0}}">+{{vo.use_money}}</view>
                  <view class='num font-color' wx:else>{{vo.use_money}}</view>
                </view>
            </view>
          </view>
      </view>
      <view class='loadingicon acea-row row-center-wrapper'>
        <text class='loading iconfont icon-jiazai' hidden='{{loading==false}}'></text>{{loadTitle}}
     </view>
    </view>
</view>
<authorize bind:onLoadFun='onLoadFun'></authorize>